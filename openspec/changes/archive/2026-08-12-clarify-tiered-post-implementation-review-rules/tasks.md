# 任务：分级实施后复盘治理

> 核心规则已重新设计。只有在修订后的正式提案获得用户明确批准后，才执行生效治理文件的同步任务。

## 0. 修订正式提案

- [x] 将双轴模型修订为“提案等级、复盘强度、工具启用”三层模型。
- [x] 将 `architecture` 默认强度修订为 `R3 Comprehensive`。
- [x] 定义安全敏感、重大实现和普通 `R2` 风险的边界。
- [x] 定义 Oracle 首选与不可用回退规则。
- [x] 定义完整五路复盘与完整 `review-work` 的独立门槛。
- [x] 取得用户对修订后正式提案的明确批准。

## 1. 同步治理入口

- [x] 更新 `AGENTS.md`，加入三层模型、强度矩阵和工具门禁。
- [x] 更新 `openspec/README.md`，解释四级默认强度和独立风险升级。
- [x] 更新 `openspec/templates/proposal-levels.md`，增加强度、Oracle 回退和 `review-work` 授权记录字段。
- [x] 更新 `openspec/templates/review-checklist.md`，增加强度判定、五视角覆盖与工具授权检查。

## 2. 一致性验证

- [x] 核对 spec、`AGENTS.md`、README 与模板措辞一致。
- [x] 验证 `fast` 只执行 `R0`。
- [x] 验证仍属于 `light` 的复杂或版本敏感事项执行 `R1`，Oracle 不可用时有明确回退。
- [x] 验证 `full` 按风险执行 2-3 路 `R2`，不会仅因普通风险自动扩大为五路。
- [x] 验证 `architecture`、安全敏感或重大实现执行 `R3` 五视角。
- [x] 验证 `R3` 不会自动调用完整 `review-work`。
- [x] 验证只有用户明确要求全面复盘、完整 QA、指定 `review-work` 或更高优先级指令时才启用完整 `review-work`。
- [x] 验证测试、构建、静态检查和失败处理义务未被削弱。
- [x] 检查 Git diff，确认未修改任何运行时、生成器、构建、CLI 或 Projects 行为文件。

## 3. 实施后复盘

- [x] 目标与约束视角：确认规则与用户确认的边界一致。
- [x] 技术质量视角：检查规范可执行性、无自相矛盾和明确术语。
- [x] 安全视角：确认安全敏感触发器不会被低等级绕过。
- [x] QA 视角：用代表场景验证矩阵和门禁。
- [x] 上下文视角：确认与既有 OpenSpec 治理和优先级规则兼容。

## 4. 总结

- [x] 记录实际治理文件、迁移状态、验证场景、五视角 verdict、剩余风险与未完成事项。
