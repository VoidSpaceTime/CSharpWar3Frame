# 提案：AGENTS.md 记录「原生句柄引用配对规则」检查提醒

**状态**：已批准（用户 2026-08-31 指示"这个可以作为检查提醒的方式记录进去"）→ 已实施
**等级**：fast
**提案日期**：2026-08-31

---

## 目标

在 `AGENTS.md` 记录原生句柄引用配对规则，作为代码审查时的检查提醒：创建原生对象后必须 `HandleAdd` 登记，销毁前必须 `HandleRemove` 注销，配对在同一 Native 系统内相邻完成，同一对象类型销毁路径唯一。

## 影响文件

- `AGENTS.md`：新增「原生句柄引用配对规则（检查提醒）」章节（位于「Native 同步三模式规则」之后、「执行要求」之前）。

## 低风险理由

- 纯文档增量，不改变任何代码行为。
- 规则内容与既有「War3 原生调用分层规则」一致（原生副作用集中在 Native 层），只是补充句柄引用管理约束。
- 可快速回滚（删除章节即可）。

## 验证方式

- 文档结构检查：章节位置与格式符合 AGENTS.md 既有风格。
- `git diff` 核对仅 AGENTS.md 变更。