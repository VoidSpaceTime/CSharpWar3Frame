# 实施总结

## 改动范围

- `AGENTS.md`：新增「原生句柄引用配对规则（检查提醒）」章节，位于「Native 同步三模式规则」之后、「执行要求」之前。内容为 4 条代码审查检查项：创建后立即 `HandleAdd`、销毁前相邻 `HandleRemove`、同一对象类型唯一销毁执行点、新增类型时创建/销毁系统成对添加。

## 验证结果

- 文档结构检查通过：章节位置与格式符合 AGENTS.md 既有风格。
- `git diff` 确认仅 `AGENTS.md` 一处变更（+10 行），无其他文件改动。

## 遗留风险与后续事项

- 无。规则为纯文档增量，不改变代码行为；后续新增原生对象类型时按检查项核对即可。